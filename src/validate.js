const validate = values => {
  const errors = {};

  if(values.horse) {
   /* if (!values.horse.name) {
      errors.horse.name = 'Required';
    }
    if (!values.lastName) {
      errors.lastName = 'Required';
    }*/
  }


  return errors;
};

export default validate;
